# CLAUDE.md

> 工地日常管理小程序 / P0 开发协作说明
>
> 本文件用于 Claude Code 在本仓库中的长期开发约束。进入项目后，先阅读本文件，再阅读现有代码、`README`、`pom.xml`/`build.gradle`、数据库脚本和当前 Git diff，然后再开始修改。

---

## 1. 项目目标

本项目是一套面向小型施工队、劳务承包团队、装修/零星工程项目的轻量级工地日常管理系统。

一期 P0 的目标不是做完整工程 ERP，也不是做法定财务会计系统，而是优先跑通以下业务闭环：

1. 项目与组织
2. 签到与考勤
3. 施工日报
4. 费用与报账
5. 经营报表

目标用户主要包括：老板/企业负责人、项目经理/项目负责人、班组长/施工员、工人/现场人员、财务/出纳，以及只读用户。

产品形态：

- 微信小程序：现场签到、日报、拍照、报账、审批、个人查询。
- Web 管理后台：项目/人员维护、权限配置、审批配置、复杂查询、经营报表、导出、审计。
- Spring Boot 后端：提供 REST/JSON API，负责认证、权限、业务规则、审批、数据统计和文件元数据管理。

---

## 2. 需求优先级与范围控制

### 2.1 P0 是当前唯一默认开发范围

除非用户明确要求，不要主动实现 P1/P2 功能。

P0：

- 项目、班组、人员、岗位/角色、成员关系、审批关系
- 签到、签退、请假、补卡、加班、日/月考勤汇总
- 施工日报：内容、工程量、人员、照片、问题、次日计划、审核
- 费用报销、借款、核销、审批、付款登记、票据附件
- 项目收入/支出、费用分类、应收应付、人工费等内部经营统计
- 基础登录、数据权限、操作审计、异常处理、测试和部署

### 2.2 当前不主动实现

以下内容不是当前 P0 开发重点，除非用户明确追加：

- 复杂采购与材料库存、仓储批次/库位/条码/多级调拨
- 任务整改完整闭环、复杂消息中心
- 合同、开票、进度款、签证变更的完整合同系统
- 设备机械完整台账
- 工资个税、社保、公积金专业核算
- 完整会计总账、凭证、资产负债表、利润表、税务申报
- BIM、复杂工程算量、造价清单、网络进度计划
- 人脸识别、活体检测、复杂门禁硬件
- 银行自动付款、电子发票查验、OCR 自动识票
- 与大型 ERP、政府实名制、银行、发票平台的深度对接

### 2.3 需求冲突处理原则

需求分析初稿中有部分“一期”扩展项，但每日开发计划明确将 P0 收敛为上述 5 大模块。

因此：

1. **开发范围以 P0 每日开发计划为第一优先级。**
2. 需求分析说明书用于补充字段、规则、状态、验收场景和后续扩展方向。
3. 遇到文档冲突时，不要自行扩大范围；先指出冲突并采用更小的 P0 范围。
4. 不允许为了“以后可能需要”提前建设复杂 ERP 式框架。

---

## 3. 技术栈约束

用户已确定后端使用：

- Java
- Spring Boot
- MyBatis-Plus

若仓库已有版本和依赖，以仓库现状为准，不主动升级大版本、不替换技术栈。

如果是全新空项目，默认建议：

- JDK 17+
- Spring Boot 3.x
- MyBatis-Plus 3.x
- MySQL 8.x
- Maven（若仓库已使用 Gradle，则继续使用 Gradle）
- Spring Validation
- Jackson
- JUnit 5

可选能力必须按需要引入，不要一开始全部安装：

- Redis：验证码、短期缓存、分布式幂等或会话确有需要时再加
- 对象存储：照片、票据、付款凭证等附件
- OpenAPI/Swagger：项目需要接口联调时使用
- 消息队列：P0 默认不引入；简单提醒优先定时任务/数据库待办

认证框架（如 Spring Security、Sa-Token、JWT 方案）如果仓库已经存在就沿用；空项目不要擅自引入多个认证框架，先保持认证层可替换并与业务层解耦。

---

## 4. 推荐后端目录结构

优先按**业务模块纵向拆分**，不要把所有 Controller、Service、Mapper 混在全局大目录里。

示例：

```text
src/main/java/<base-package>/
├── common/
│   ├── api/              # Result、PageResult、错误码
│   ├── exception/        # 全局异常
│   ├── model/            # BaseEntity 等
│   ├── util/
│   └── constants/
├── config/
├── auth/                 # 登录、当前用户、权限上下文
├── module/
│   ├── project/          # 项目与组织
│   │   ├── controller/
│   │   ├── service/
│   │   ├── mapper/
│   │   ├── entity/
│   │   ├── dto/
│   │   ├── vo/
│   │   └── enums/
│   ├── attendance/       # 考勤
│   ├── dailyreport/      # 施工日报
│   ├── expense/          # 报销、借款、核销、付款
│   ├── approval/         # 简单审批能力
│   ├── report/           # 经营报表
│   └── audit/            # 审计日志
└── WorksiteApplication.java
```

规则：

- 包名以现有项目的 `groupId` / 根包名为准。
- 一个业务模块内部维护自己的 Entity / DTO / VO / Mapper / Service。
- Controller 只做参数接收、鉴权入口、调用 Service、返回结果。
- 业务规则必须放在 Service/Domain 层，不能堆在 Controller。
- Mapper 只负责数据访问，不在 Mapper 里写业务状态判断。

---

## 5. API 设计规范

### 5.1 REST 风格

统一前缀建议：

```text
/api/v1
```

示例：

```text
GET    /api/v1/projects
POST   /api/v1/projects
GET    /api/v1/projects/{id}
PUT    /api/v1/projects/{id}
POST   /api/v1/projects/{id}/members
POST   /api/v1/attendance/check-in
POST   /api/v1/attendance/check-out
POST   /api/v1/leave-requests
POST   /api/v1/daily-reports
POST   /api/v1/expense-claims
POST   /api/v1/expense-claims/{id}/submit
POST   /api/v1/approvals/{id}/approve
POST   /api/v1/approvals/{id}/reject
POST   /api/v1/payments
GET    /api/v1/reports/project-operation
```

### 5.2 返回结构

统一返回，不要每个 Controller 自定义格式。

```java
Result<T>
PageResult<T>
```

建议包含：

- `code`
- `message`
- `data`
- `requestId`（已有链路追踪时）

### 5.3 DTO / VO

- Entity 不直接作为 API 入参或出参。
- 新增、修改、查询条件分别使用 DTO。
- 返回前端使用 VO。
- 所有外部输入使用 Bean Validation。
- 金额字段使用 `BigDecimal`，禁止 `double/float`。
- 日期使用 `LocalDate`，时间使用 `LocalDateTime`。
- 考勤判定以**服务端时间**为准，不信任客户端上报的当前时间。

---

## 6. MyBatis-Plus 使用规范

1. Mapper 默认继承 `BaseMapper<T>`。
2. 优先使用 `LambdaQueryWrapper` / `LambdaUpdateWrapper`，避免字符串列名。
3. 简单 CRUD 使用 MyBatis-Plus；复杂统计、经营报表、多表聚合使用明确 SQL/XML，不要强行拼 Wrapper。
4. 分页统一使用 MyBatis-Plus 分页插件。
5. 禁止 Controller 直接调用 Mapper。
6. 写操作涉及多表状态变化时使用：

```java
@Transactional(rollbackFor = Exception.class)
```

7. 不要在循环中逐条查询造成 N+1；批量加载后在内存映射，或写一次关联查询。
8. 列表查询必须检查 `company_id` / `project_id` / 当前用户数据权限，不能只按前端传入 ID 查询。
9. 不可信任客户端传入的 `companyId` 作为权限依据；企业/项目范围必须从当前登录用户授权关系计算。
10. 关键状态更新需要带原状态条件，防止并发重复审批/重复付款。
11. 对高风险状态流转可使用乐观锁 `version` 或条件更新。
12. XML 中禁止 `SELECT *`，明确字段。

---

## 7. 数据库通用规范

### 7.1 命名

- 表名、字段名：`snake_case`
- Java：`camelCase`
- 主键：`id BIGINT`
- MyBatis-Plus 可使用 `ASSIGN_ID`，若项目已有 ID 生成规则则沿用

### 7.2 通用字段

业务主表通常考虑：

```text
id
company_id
project_id            # 项目级数据需要
created_by
created_at
updated_by
updated_at
version               # 并发状态更新需要时
deleted               # 仅适合允许逻辑删除的数据
```

注意：

- 已审批、已付款等业务单据不能通过逻辑删除来“消失”。
- 此类数据必须通过 `voided/作废/冲销/更正` 等业务状态保留痕迹。
- 项目结束后可归档或只读，不应物理删除历史记录。

### 7.3 字段类型

- 金额：`DECIMAL(14,2)` 或根据现有系统统一精度
- 数量/工程量：使用 `DECIMAL`，不要用浮点数
- 经纬度：使用足够精度的 `DECIMAL`
- 状态：数据库存稳定 code；Java 使用 Enum 映射
- 手机号等敏感字段按权限返回，必要时脱敏

### 7.4 索引原则

至少考虑：

- `company_id`
- `project_id`
- `user_id/person_id`
- `biz_date`
- `status`
- `(project_id, biz_date)`
- 高频审批待办组合索引

唯一约束/业务幂等要在数据库层配合实现，不能只依赖前端防重复点击。

---

## 8. P0 核心领域模型

以下是建议模型，不要求 Day 4 一次性创建全部表；应按当天开发任务逐步落地。

### 8.1 项目与组织

核心对象：

- `company`：企业
- `project`：项目
- `team`：班组
- `person`：人员档案
- `project_member`：人员参与项目关系
- `role` / `user_role`：角色
- `permission` / `role_permission`：菜单/操作权限（如项目需要）
- `approval_config`：审批人/审批规则配置

项目核心字段：

- 项目编号
- 项目名称
- 地址
- 负责人
- 开工日期
- 计划完工日期
- 状态
- 签到定位中心点和允许范围（考勤需要时）

项目状态建议：

```text
PREPARING -> ACTIVE -> PAUSED -> COMPLETED -> SETTLED -> ARCHIVED
筹备       -> 在建   -> 暂停   -> 完工      -> 已结算    -> 已归档
```

### 8.2 签到与考勤

核心对象：

- `attendance_shift`：班次/考勤规则
- `attendance_record`：签到签退记录
- `leave_request`：请假
- `attendance_correction`：补卡
- `overtime_request`：加班

签到记录至少包含：

- 人员
- 项目
- 考勤日期
- 签到/签退服务端时间
- 经度/纬度
- 地址文本
- 现场照片附件
- 距离项目签到点的距离（如启用范围校验）
- 正常/迟到/早退/缺卡/异常等状态
- 设备/客户端信息（需要时）

关键规则：

- 定位范围可按项目配置。
- 签到时间以服务端时间为准。
- 同一人员同一时间不能在两个项目同时形成正常签到。
- 定位失败不能伪造正常签到，应允许重试或进入异常/补卡流程。
- 补卡审批后更新统计结果，但必须保留原始异常记录和审批痕迹。
- 请假与签到时间冲突时给出提示。
- 加班可由员工申请或负责人登记，最终按审批/确认结果进入统计。

### 8.3 施工日报

核心对象：

- `daily_report`
- `daily_report_work_item`
- `daily_report_labor`
- `daily_report_attachment`

日报至少支持：

- 日期、项目、填写人、天气
- 施工区域/楼栋/楼层/部位
- 工序、工种、工作内容
- 实际完成工程量、计量单位
- 班组、人数、工时
- 机械名称/台班（P0 可简化为文本/明细）
- 现场照片
- 施工问题、安全/质量问题、待协调事项
- 次日计划
- 草稿、提交、审核、退回

状态建议：

```text
DRAFT -> SUBMITTED -> APPROVED
                  -> REJECTED / RESUBMITTED
```

必须保证：

- 草稿内容不会因网络中断轻易丢失。
- 审核退回后可修改再提交。
- 日报人数可与当天考勤人数做差异提示，但 P0 不强制拦截。

### 8.4 费用、报销、借款、核销与付款

核心对象：

- `expense_claim`
- `expense_claim_item`
- `expense_attachment`
- `loan`
- `loan_writeoff`
- `approval_instance`
- `approval_record`
- `payment_record`

费用单至少包含：

- 项目（必填）
- 费用类别
- 发生日期
- 申请人
- 多条费用明细
- 数量、单价、金额
- 收款方/商户/账户
- 支付方式
- 事由
- 发票/收据/付款截图/现场凭证等附件
- 关联借款（核销时）
- 审批状态、付款状态

关键规则：

- 所有报账必须关联项目；公司公共费用可使用专门的“公司公共项目”，不要留空项目。
- 一张报销单可包含多条明细，总金额由服务端汇总计算。
- 金额、数量不得为负数。
- 审批金额阈值必须配置化，**不要把 500/5000 等示例金额写死在代码里**。
- 同一申请人不能审批自己的单据。
- 已付款单据不允许物理删除，只能作废/冲销并保留日志。
- 报销关联借款时，服务端计算已核销、应退回、应补付、剩余未核销金额。
- 疑似重复单据应提示风险：可结合项目、日期、金额、收款方、票据文件 hash 等判断；P0 先做简单可解释规则，不做复杂 AI 识别。

单据状态建议：

```text
DRAFT
-> SUBMITTED
-> APPROVING
-> APPROVED
-> WAITING_PAYMENT
-> PAID
-> ARCHIVED
```

旁路状态：

```text
REJECTED / WITHDRAWN / VOIDED
```

### 8.5 经营报表

P0 报表是**项目经营管理台账**，不是法定会计报表。

建议输出：

- 项目累计收入
- 项目累计支出
- 收支差额
- 人工费、材料费、机械费、差旅费等费用分类
- 应收款
- 应付款
- 待报销
- 未核销借款
- 人工费统计
- 按项目、日期、人员、班组筛选
- Excel 导出

重要口径：

```text
现金净流入 = 已收款 - 已付款
应收余额   = 应收计划/已确认应收 - 已收款
应付余额   = 已确认应付 - 已付款
```

如果后续增加“经营毛利”，必须明确：

```text
经营毛利 = 已确认收入 - 已发生的人工/材料/机械/分包/其他成本
```

不要把“现金净流入”和“经营毛利”混成一个“利润”字段。

报表查询应尽量直接从业务事实表聚合，避免重复记账形成第二套无法核对的数据源。

---

## 9. 权限与数据隔离

权限按：

```text
企业 -> 项目 -> 班组 -> 个人
```

分层处理。

角色默认行为：

- 老板/企业负责人：查看全部授权企业的项目经营数据、汇总报表，可参与大额审批和企业级配置。
- 项目经理/负责人：维护本项目成员；审核本项目考勤、日报、报账等。
- 班组长/施工员：管理本班组；提交日报、工作量、特殊人员记录。
- 工人/现场人员：签到、请假、查看本人考勤，提交本人相关单据。
- 财务/出纳：费用复核、付款登记、费用类别维护、报表与对账。
- 只读人员：只能查看被授权数据，不可修改、审批、付款。

强制规则：

1. 普通用户默认只能看本人相关数据。
2. 跨项目访问必须有明确授权。
3. 费用金额、工资、身份证、银行卡等敏感信息按角色控制或脱敏。
4. 提交、审批、付款尽量由不同角色完成。
5. 所有审批、付款、撤回、作废等关键动作必须留审计日志。
6. 权限校验必须在后端完成，不能只靠前端隐藏按钮。
7. 导出接口同样需要数据权限校验。

---

## 10. 审批设计原则

P0 只做**可配置单线审批或简单多级审批**，不要开发 BPMN/工作流平台。

审批能力至少支持：

- 业务类型：请假、补卡、加班、报销、借款
- 发起人
- 当前审批节点
- 审批人/审批角色
- 审批通过
- 退回/驳回
- 撤回
- 审批意见
- 全量审批记录
- 完成后的业务状态回写

审批规则：

- 金额阈值可配置。
- 审批关系可按企业/项目配置。
- 自己不能审批自己的费用单。
- 每次动作记录操作人、时间、动作、意见、前后状态。
- 所有状态迁移都在事务内完成。
- 重复点击“通过/付款”必须幂等。

---

## 11. 附件与图片

工地场景有大量：

- 签到照片
- 施工现场照片
- 发票/收据
- 付款截图
- 其他凭证

规则：

1. 数据库只保存文件元数据、对象存储 key、业务关联，不把大文件存数据库 BLOB。
2. 对象存储 URL 不应长期公开；优先受控/签名访问。
3. 上传失败需要明确失败状态，支持重试。
4. 小程序端可压缩图片，但服务端仍需校验大小、类型和数量。
5. 删除附件要检查业务单据状态；已审批/已付款单据附件不能被普通修改接口直接删除。
6. 必要时保存文件 hash，用于重复票据风险提示。

---

## 12. 弱网、幂等与异常处理

现场网络可能不稳定，因此：

- 表单允许草稿。
- 提交失败必须明确显示“未提交成功”。
- 客户端可重试，但服务端必须防止重复创建。
- 对报销提交、审批、付款、签到等关键操作设计幂等策略。
- 可使用业务唯一键 / requestId / 数据库唯一索引实现幂等。
- 不允许仅依赖前端按钮 disable。
- 定位失败、图片失败、审批中断必须有可恢复路径。

---

## 13. 审计日志

以下行为至少应记录审计：

- 新增/修改关键主数据
- 项目成员调整
- 权限调整
- 提交
- 审批通过/退回
- 撤回
- 作废
- 付款登记
- 关键金额修改
- 批量导出（如实现）

建议审计字段：

```text
biz_type
biz_id
action
operator_id
operator_name
before_data    # 需要时
next_data      # 需要时
remark
ip/device      # 能获取时
created_at
```

不要在日志中明文记录密码、token、完整银行卡号等敏感数据。

---

## 14. 非功能要求

开发时要持续考虑：

- 常规列表/详情正常网络下尽量在 2 秒内展示主要内容。
- P0 预留单企业约 1000 用户、约 200 人同时在线的量级，不做过度分布式设计。
- 照片支持压缩和失败重传。
- 数据库需要备份策略。
- 关键附件使用对象存储。
- 月可用性目标可按 99.5% 设计，但 P0 不为此堆砌复杂微服务。
- 敏感接口：登录、权限、参数校验、必要的频率限制。
- 定位和照片属于敏感能力，前端需要明确授权说明。

架构原则：**优先可靠、简单、可维护，不为了技术炫技拆微服务。P0 默认单体 Spring Boot 应用即可。**

---

## 15. 开发计划：36 个工作日

用户说“开始第 N 天”时，优先完成对应任务，不提前大范围开发后续功能。

### 第 1—4 天：需求确认与基础框架

- Day 1：确认项目、人员、考勤、日报、报销、经营报表现有流程和痛点。
- Day 2：确认角色、数据范围、审批关系和 P0 边界。
- Day 3：确认小程序首页、项目、签到、日报、报销、报表核心页面原型。
- Day 4：建立数据库、Spring Boot 后端、微信小程序/后台基础框架、登录框架、数据库连接、接口基础结构。

验收：基础工程可以启动，数据库连接正常，登录/API 基础结构可用。

### 第 5—8 天：项目与组织

- Day 5：项目创建、编辑、状态、负责人、开工/完工日期、详情。
- Day 6：班组、人员档案、工种/岗位、手机号、人员加入项目。
- Day 7：角色、菜单权限、操作权限、项目数据权限。
- Day 8：审批人配置、人员启停、项目成员调整、模块测试。

验收：项目、班组、人员、角色和审批关系可配置，普通用户不能越权访问。

### 第 9—14 天：签到与考勤

- Day 9：签到、签退、当天考勤状态、个人记录。
- Day 10：项目定位、现场照片、地址、签到范围判断。
- Day 11：请假类型、起止时间、原因、附件和审批。
- Day 12：补卡、漏签说明、异常处理、补卡审批。
- Day 13：加班、工时统计、迟到早退、考勤明细。
- Day 14：日/月汇总、筛选、导出基础数据、重复签到/无定位/跨天异常测试。

验收：签到、请假、补卡、加班、汇总形成闭环，异常记录可追踪。

### 第 15—19 天：施工日报

- Day 15：新建、编辑、草稿、提交、列表。
- Day 16：工作内容、施工区域/楼层、工序、工程量、计量单位。
- Day 17：班组、人数、工时、机械/台班、现场负责人。
- Day 18：照片、天气、施工问题、安全/质量问题、待协调事项、明日计划。
- Day 19：查询、审核、退回、按项目/日期汇总、模块测试。

验收：日报完整记录工作、人员、工程量、照片、问题、计划，并支持审核与查询。

### 第 20—26 天：费用与报账

- Day 20：费用类别、报销单、报销人、项目、发生日期、说明。
- Day 21：多条明细、金额、数量、单价、收款账户、费用归属、合计金额。
- Day 22：发票、收据、付款截图、现场凭证上传/预览/删除。
- Day 23：借款申请、用途、预计核销日期、审批、借款台账。
- Day 24：借款核销、报销冲抵、退回余额、应补/应退/未核销计算。
- Day 25：简单多级审批、意见、退回修改、撤回、审批记录、待办。
- Day 26：付款登记、付款时间、方式、凭证、报销查询、完整模块测试。

验收：报销、借款、核销、审批、付款数据一致，金额正确，全程可追溯。

### 第 27—30 天：经营报表

- Day 27：项目收入、支出、明细和收支汇总。
- Day 28：人工/材料/机械/差旅等费用分类与趋势汇总。
- Day 29：应收、应付、待报销、未核销借款、人工费统计。
- Day 30：按项目/日期/人员/班组筛选，Excel 导出，核对统计口径。

验收：经营报表口径与业务负责人确认一致，筛选、明细下钻和导出结果正确。

### 第 31—36 天：联调、测试与上线

- Day 31：小程序、管理后台、后端整体联调；走通“项目创建 -> 人员加入 -> 签到 -> 日报 -> 报销 -> 审批 -> 报表”。
- Day 32：测试菜单、操作、项目数据和审批权限，修复越权/漏权。
- Day 33：测试重复提交、异常签到、金额边界、附件失败、审批中断等异常场景。
- Day 34：页面交互、响应、空状态、错误提示、图片压缩、弱网体验优化。
- Day 35：初始化项目、人员、角色、费用类别、审批关系和演示数据；形成操作说明。
- Day 36：正式部署、数据库备份、小程序审核、用户验收和交付。

验收：核心流程正式环境稳定，权限正确，验收问题有清单、有结论。

---

## 16. 关键验收场景

实现功能时应优先覆盖这些场景：

1. 新员工加入项目并通过负责人审核后才能正常使用项目权限；未审核前不可查看敏感数据。
2. 员工在项目范围签到/签退，正常生成正常、迟到、早退等状态。
3. 定位失败或缺卡时可以发起异常/补卡申请；审批后统计更新，同时保留原记录。
4. 班组长提交含人员、工作量、照片、问题的日报；项目经理可以通过或退回。
5. 员工提交报销，服务端校验项目、金额、类别、附件，并进入正确审批链。
6. 财务付款后，单据进入已付款状态，项目支出/现金流同步变化。
7. 高度相似的重复报销得到风险提示。
8. 老板可以查看项目收入、支出、应收、应付、现金净流入并下钻明细。
9. 普通员工不能查看他人的费用、敏感工资和联系方式等未授权信息。
10. 修改、审批、作废、付款等关键动作可追溯到具体操作人和时间。

---

## 17. 测试要求

每个模块至少覆盖：

- 正常流程
- 权限不足
- 非法状态流转
- 重复提交
- 空值/边界值
- 金额精度
- 事务回滚
- 数据隔离

重点测试：

- 普通用户越权访问其他项目/人员数据
- 重复签到/签退
- 跨天签到
- 定位失败
- 请假与考勤冲突
- 补卡后统计正确且保留历史
- 日报退回再提交
- 报销金额合计
- 借款核销金额
- 自己审批自己的报销
- 重复审批
- 重复付款
- 已付款单据非法删除/修改
- 报表金额是否与业务明细一致

测试框架优先使用项目已有方案；空项目默认 JUnit 5 + Spring Boot Test，Controller 接口可使用 MockMvc。

---

## 18. 编码规范

1. 代码命名使用清晰英文业务名，中文只用于注释/文档/错误提示。
2. 不使用 `Map<String, Object>` 代替明确 DTO，除非动态报表确有需要。
3. 不写超大 Service；一个方法只做一个明确业务动作。
4. 复杂金额计算拆成独立方法并写测试。
5. 禁止魔法数字；状态、类型、阈值使用枚举/配置。
6. 禁止吞异常；统一业务异常和全局异常处理。
7. 日志使用参数化写法，不字符串拼接。
8. 不在日志打印 token、密码、完整身份证/银行卡等敏感数据。
9. 注释解释“为什么”，不要逐行翻译代码。
10. 修改现有代码时遵循原有风格，不进行与当前任务无关的大规模重构。

---

## 19. Claude Code 每次工作的执行协议

收到开发任务后，按以下顺序执行：

### Step 1：定位当前任务

先判断用户是在做：

- 哪一天（Day N）
- 哪个模块
- 后端 / 小程序 / 管理后台 / 数据库 / 联调中的哪一部分

如果用户只说“继续”，结合 Git diff、TODO、最近代码判断，不要重新搭一套。

### Step 2：先读再改

开始编码前必须先检查：

- 本 `CLAUDE.md`
- `pom.xml` / `build.gradle`
- `application*.yml`
- 当前模块已有代码
- 数据库脚本
- Git diff / Git status

禁止在未了解现有结构时擅自创建第二套架构。

### Step 3：给出小范围实现计划

计划控制在 3～8 个步骤，明确：

- 要改哪些文件
- 是否新增表/字段
- 是否有接口变化
- 是否影响权限、状态、报表口径

### Step 4：实施

- 一次优先完成一个可验证闭环。
- 不提前实现后续 P1/P2。
- 不因为当前功能简单就引入重量级框架。
- 数据库变更必须提供可追踪 SQL。

如果项目已经使用 Flyway/Liquibase，就按现有方式新增 migration；如果没有，不要擅自引入，先把版本化 SQL 放在项目已有 SQL 目录中。

### Step 5：自测

改完至少执行项目已有的：

```bash
mvn test
```

或等价 Gradle 命令。

如果是全量改动，建议：

```bash
mvn clean test
```

不能运行测试时，要明确说明原因，不要声称“测试已通过”。

### Step 6：汇报

每次完成后简洁说明：

1. 完成了什么
2. 修改了哪些关键文件
3. 数据库是否变化
4. 测试结果
5. 仍有哪些风险/TODO
6. 下一步建议对应 Day N 的哪一项

---

## 20. 禁止事项

Claude 在本项目中不要：

- 未经要求扩大到完整 ERP
- 未经要求改成微服务
- 未经要求替换 Spring Boot / MyBatis-Plus
- 为简单审批引入复杂 BPM 引擎
- 将 Entity 直接暴露给前端
- 用 `double` 处理金额
- 信任前端传入的企业/项目权限范围
- 允许普通接口物理删除已审批/已付款单据
- 省略操作审计
- 硬编码审批金额阈值
- 只做前端权限隐藏而没有后端校验
- 为追求“优雅”进行与当前任务无关的大规模重构
- 在没有测试或编译结果时宣称功能可用

---

## 21. 当前里程碑

- Day 8：基础框架 + 项目/人员/权限完成
- Day 14：签到考勤完成，可小范围现场试用
- Day 19：施工日报完成
- Day 26：费用报账 + 借款核销 + 付款闭环完成
- Day 30：P0 业务功能全部完成，进入综合测试
- Day 36：部署、验收、正式交付

---

## 22. 当前未确认项

以下属于业务待确认，不要偷偷假设成固定规则：

- 实际工程类型、项目/班组/人员规模
- 考勤是按天、小时还是混合统计
- 签到范围半径
- 是否强制现场照片
- 是否允许班组长代签/批量补录
- 日报具体必填字段
- 报销类别
- 实际审批顺序和金额门槛
- 项目收入/应收确认规则
- 老板最终需要的经营报表口径
- 哪些金额/个人字段需要隐藏
- 是否需要外部甲方/监理只读分享

遇到这些规则影响数据库或核心逻辑时，优先做成可配置；如果无法合理配置，先向用户确认。

---

## 23. 最终开发原则

本项目的优先顺序：

```text
业务闭环正确
> 权限和金额安全
> 数据可追溯
> 现场易用与弱网可恢复
> 代码可维护
> 性能优化
> 技术炫技
```

始终围绕“让真实工地人员愿意使用、让数据能够核对、让老板/项目负责人能看懂”来做 P0。
